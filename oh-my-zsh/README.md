# ZSH

## Configuration

1. Create a symlink
   ```bash
      ln -s $(pwd)/.zshrc ~/.zshrc
      ln -s $(pwd)/starship.toml ~/.config/starship.toml
   ```

1. After any changes, execute:
```bash
exec zsh
```

